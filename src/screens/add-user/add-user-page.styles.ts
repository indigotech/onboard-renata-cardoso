import {StyleSheet} from 'react-native';

export const styleAddUser = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  textError: {
    color: '#FF0000',
    fontSize: 14,
    marginTop: 14,
  },
});
