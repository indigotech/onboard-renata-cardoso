import {StyleSheet} from 'react-native';

export const styleUser = StyleSheet.create({
  container: {
    padding: 8,
    borderWidth: 1,
    borderColor: '#000000',
  },
  text: {
    fontSize: 16,
  },
});
