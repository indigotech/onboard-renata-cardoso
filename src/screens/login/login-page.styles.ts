import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  header: {
    fontSize: 32,
    fontWeight: 'bold',
    color: '#000',
    textAlign: 'center',
    marginTop: 60,
  },
  wrapperInput: {
    marginTop: 32,
  },
  textError: {
    color: '#FF0000',
    fontSize: 14,
    marginTop: 14,
  },
});
